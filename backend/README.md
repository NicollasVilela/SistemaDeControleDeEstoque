# Backend - Estoque Inteligente

API REST desenvolvida com FastAPI para controle de estoque e regras de precificação.

## Rodar localmente

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
```

Acesse a documentação em:

```text
http://localhost:8000/docs
```
